<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/x-icon" href="../assets/favicon.ico" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="stylesheet" href="/styles/leaderboard.css" />
  <title>MindHive · Leaderboard</title>
</head>

<body>
  <%- include('partials/sidebar', { page: 'leaderboard' }) %>
  <%- include('partials/pomodoro_popup') %>

  <div class="leaderboard-container">
    
    <h1>🏆 Leaderboard</h1>

    <div class="lb-card">
      <div class="lb-row lb-header">
        <div class="lb-col rank-col">#</div>
        <div class="lb-col user-col">User</div>
        <div class="lb-col points-col">Points</div>
      </div>

      <!-- JS will fill this dynamically -->
      <div id="leaderboard-body"></div>
    </div>

    <div class="lb-pagination">
      <button
        id="paginationPrev"
        class="lb-page-btn"
        <%= page <= 1 ? "disabled" : "" %>>
        <i class="fa-solid fa-chevron-left"></i>
      </button>

      <span class="lb-page-number">
        Page <%= page %> / <%= totalPages %>
      </span>

      <button
        id="paginationNext"
        class="lb-page-btn"
        <%= page >= totalPages ? "disabled" : "" %>>
        <i class="fa-solid fa-chevron-right"></i>
      </button>
    </div>

  </div>

  <script src="/js/leaderboard.js"></script>
  <script src="/js/pomodoro.js"></script>
</body>
</html>