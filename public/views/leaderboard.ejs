<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/svg+xml" href="/assets/logo.svg" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="stylesheet" href="/styles/leaderboard.css" />
  <title>MindHive · Leaderboard</title>
</head>

<body>
  <%- include('partials/sidebar', { page: 'leaderboard' }) %>

  <div class="leaderboard-container">
    
    <h1>🏆 Leaderboard</h1>

    <!-- ================= FULL TABLE CARD ================= -->
    <div class="lb-card">

      <!-- Header -->
      <div class="lb-row lb-header">
        <div class="lb-col rank-col">#</div>
        <div class="lb-col user-col">User</div>
        <div class="lb-col points-col">Points</div>
      </div>

      <!-- ROWS -->
      <% if (users && users.length > 0) { %>

        <% users.forEach((user, i) => { %>

          <div class="lb-row lb-item">

            <!-- Rank -->
            <div class="lb-col rank-col">
              <%= user.position %>
            </div>

            <!-- Avatar + Name + Username -->
            <div class="lb-col user-col user-flex">

              <% if (user.profile_image) { %>
                <img class="lb-avatar" src="<%= user.profile_image %>" />
              <% } else { %>
                <div class="lb-avatar-initials">
                  <%= user.first_name.charAt(0).toUpperCase() + user.last_name.charAt(0).toUpperCase() %>
                </div>
              <% } %>

              <div class="lb-info">
                <p class="lb-name">
                  <%= user.first_name %> <%= user.last_name %>
                </p>
                <p class="lb-username">
                  @<%= user.username %>
                </p>
              </div>

            </div>

            <!-- Points -->
            <div class="lb-col points-col">
              <%= user.total_points %> pts
            </div>

          </div>

        <% }) %>

      <% } else { %>
        <div class="lb-empty">No ranked users yet.</div>
      <% } %>

    </div>

    <!-- ================= PAGINATION ================= -->
    <div class="lb-pagination">

      <button
        id="paginationPrev"
        class="lb-page-btn"
        <%= page <= 1 ? "disabled" : "" %>
        data-page="<%= page - 1 %>"
      >
        <i class="fa-solid fa-chevron-left"></i>
      </button>

      <span class="lb-page-number">
        Page <%= page %> / <%= totalPages %>
      </span>

      <button
        id="paginationNext"
        class="lb-page-btn"
        <%= page >= totalPages ? "disabled" : "" %>
        data-page="<%= page + 1 %>"
      >
        <i class="fa-solid fa-chevron-right"></i>
      </button>

    </div>

  </div>

  <script src="/js/leaderboard.js"></script>
</body>
</html>