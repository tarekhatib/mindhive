<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/svg+xml" href="/assets/logo.svg" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="stylesheet" href="/styles/leaderboard.css" />
  <title>MindHive · Leaderboard</title>
</head>

<body>
  <%- include('partials/sidebar', { page: 'leaderboard' }) %>

  <div class="leaderboard-container">

    <h1>Leaderboard</h1>
    <div class="podium-wrapper">
      <!-- 1st place -->
      <div class="podium-card first">
        <div class="podium-rank">🥇</div>
        <img class="podium-avatar" src="<%= top3[0]?.avatar || '/assets/default-avatar.png' %>" />
        <p class="podium-username">@<%= top3[0]?.username || '-' %></p>
        <p class="podium-points"><%= top3[0]?.points || 0 %> pts</p>
      </div>

      <!-- 2nd place -->
      <div class="podium-card second">
        <div class="podium-rank">🥈</div>
        <img class="podium-avatar" src="<%= top3[1]?.avatar || '/assets/default-avatar.png' %>" />
        <p class="podium-username">@<%= top3[1]?.username || '-' %></p>
        <p class="podium-points"><%= top3[1]?.points || 0 %> pts</p>
      </div>

      <!-- 3rd place -->
      <div class="podium-card third">
        <div class="podium-rank">🥉</div>
        <img class="podium-avatar" src="<%= top3[2]?.avatar || '/assets/default-avatar.png' %>" />
        <p class="podium-username">@<%= top3[2]?.username || '-' %></p>
        <p class="podium-points"><%= top3[2]?.points || 0 %> pts</p>
      </div>

    </div>

    <div class="leaderboard-list">
      <% if (others && others.length > 0) { %>

        <% others.forEach((user, i) => { %>

          <div class="leaderboard-item">
            <div class="rank-number"><%= i + 4 %></div>

            <img class="lb-avatar" src="<%= user.avatar || '/assets/default-avatar.png' %>" />

            <div class="lb-info">
              <p class="lb-username">@<%= user.username %></p>
            </div>

            <div class="lb-points"><%= user.points %> pts</div>
          </div>

        <% }) %>

      <% } else { %>
        <p class="no-users">No ranked users yet.</p>
      <% } %>
    </div>

  </div>

  <script src="/js/leaderboard.js"></script>
</body>
</html>